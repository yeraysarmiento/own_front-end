// import { mount } from "@vue/test-utils";
// import BoardGallery from "./BoardGallery.vue";

// describe("Given a BoardGallery component", () => {
//   describe("When it receives an array of boards", () => {
//     test("Then it should render as many Boards as the array's length", () => {
//       const wrapper = mount(BoardGallery);
//       const { boards } = wrapper.vm;
//       console.log(1, boards);
//       wrapper.setProps({ boards: [...boards, { logo: "Logo", name: "Name" }] });

//       expect(wrapper.html()).toMatchSnapshot();
//     });
//   });
// });
